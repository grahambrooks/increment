//! Semantic style tokens — `added`, `removed`, `modified`, `moved`,
//! `emphasis`, `fold`, `gutter` — resolved through a theme into terminal
//! styles, with truecolor, 256-colour and 16-colour renderings of each.
//!
//! Colour is never the only channel: a `--marker` mode carries change kind in
//! glyphs, and a colourblind-safe theme ships alongside the default.
